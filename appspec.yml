version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/app0

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
hooks:
  AfterInstall:
    - location: /scripts/afterinstall.sh
      timeout: 300
      runas: root